version: "3.9"
services:
    mssql:
        image: 'mcr.microsoft.com/mssql/server'
        ports:
            - '1433:1433'
        environment:
            - ACCEPT_EULA=Y
            - SA_PASSWORD=Pass@word
        volumes:
        - my-volume:/var/opt/mssql
      
volumes:
  my-volume: